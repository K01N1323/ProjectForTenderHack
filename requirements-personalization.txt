# Recommended runtime for training: Linux + Python 3.11/3.12.
# The offline pipeline itself is stdlib-first; CatBoost is optional but required for the final ranker.
catboost>=1.2,<2.0 ; python_version < "3.13"
lightgbm>=4.0,<5.0

